# ============================================
# TalentPitch RecommendationService - Example Environment
# ============================================
# Copy this file to credentials/.env and fill in your actual values
# NEVER commit the actual credentials/.env file!

# ============================================
# Environment Selection
# ============================================
ENVIRONMENT=staging  # Options: staging or prod

# ============================================
# SSH Tunnel Configuration
# ============================================
SSH_HOST=your-ssh-host.example.com
SSH_USER=your-ssh-username

# ============================================
# Redis Configuration
# ============================================
REDIS_HOST=your-redis-host.example.com
REDIS_PASSWORD=your-redis-password
REDIS_PORT=6379
REDIS_SCHEME=tls  # Options: tls or redis

# ============================================
# MySQL Staging Environment
# ============================================
STG_MYSQL_HOST=staging-mysql-host.example.com
STG_MYSQL_USER=your-staging-mysql-user
STG_MYSQL_PASSWORD=your-staging-mysql-password
STG_MYSQL_DB=talentpitch_staging
STG_MYSQL_PORT=3306

# ============================================
# MySQL Production Environment
# ============================================
PROD_MYSQL_HOST=prod-mysql-host.example.com
PROD_MYSQL_USER=your-prod-mysql-user
PROD_MYSQL_PASSWORD=your-prod-mysql-password
PROD_MYSQL_DB=talentpitch_production
PROD_MYSQL_PORT=3306

# ============================================
# API Server Configuration
# ============================================
API_HOST=0.0.0.0
API_PORT=5002

# ============================================
# Activity Tracking & Flush Configuration
# ============================================
# How often to flush activities to MySQL (in seconds)
FLUSH_INTERVAL_SECONDS=900  # 15 minutes

# How many activities to accumulate before auto-flush
FLUSH_THRESHOLD_ACTIVITIES=50

# ============================================
# Notes
# ============================================
# - The ENVIRONMENT variable determines which MySQL credentials to use (STG_* or PROD_*)
# - SSH keys should be placed in credentials/ directory (e.g., credentials/key.pem)
# - Ensure SSH keys have proper permissions: chmod 600 credentials/*.pem

